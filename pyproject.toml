[tool.poetry]
name = "twilio-conversation-relay-aws"
version = "0.1.0"
description = "Twilio Conversation Relay Voice Agent with FastAPI and AWS integration."
authors = ["Your Name <your.email@example.com>"]

[tool.poetry.dependencies]
python = "^3.10"
fastapi = "^0.110.0"
uvicorn = "^0.29.0"
pydantic = "^2.0.0"
boto3 = "^1.34.0"
websockets = "^12.0"

[build-system]
requires = ["poetry-core>=1.0.0"]
build-backend = "poetry.core.masonry.api"

# Twilio ConversationRelay Environment Configuration

# Required: Twilio credentials
TWILIO_ACCOUNT_SID=ACxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx
TWILIO_AUTH_TOKEN=your_auth_token_here

# AWS credentials for deployment if not using default profile
# Set to your preferred profile name (not 'default')
AWS_PROFILE=devaccount
AWS_REGION=us-east-1

# Deployment configuration
STACK_NAME=twilio-conversation-relay
SERVICE_NAME=twilio-conversation-relay

# Optional: SSL/TLS Configuration (for production HTTPS)
# Get certificate ARN from AWS Certificate Manager
# CERTIFICATE_ARN=arn:aws:acm:us-east-1:123456789012:certificate/12345678-1234-1234-1234-123456789012

# Optional: Custom domain name
# DOMAIN_NAME=api.yourdomain.com

# TTS configuration
TTS_PROVIDER=ElevenLabs
TTS_VOICE=lxYfHSkYm1EzQzGhdbfc
FALLBACK_TTS=Google.en-US-Chirp3-HD-Aoede

# STT configuration
TRANSCRIPTION_PROVIDER=Deepgram
SPEECH_MODEL=nova-3-general

# Prompts
WELCOME_GREETING=Hello! I'm your AI assistant. How can I help you today?
WELCOME_ERROR_PROMPT=I'm sorry, there was an error starting the conversation. Please try again later.
ERROR_PROMPT=I'm sorry, a problem occurred while handling your call. Please try your call again.

# Idle configuration
IDLE_REMINDER=I'm still here, let me know when you are ready to continue.
IDLE_TIMEOUT_PROMPT=I'm sorry, I haven't heard you respond in a while. Please try your call again.
IDLE_MAX_ATTEMPTS=3
IDLE_TIMEOUT=20

# Additional configuration
LANGUAGE=en-US
INITIAL_HINTS=
DTMF_MAX_DIGITS=10
DTMF_TIMEOUT=3
ERROR_MAX_ATTEMPTS=5

# Development mode (allows running without HTTPS for testing)
# Set to "development" to disable strict webhook validation
# # Application Configuration
ENVIRONMENT=development
LOG_LEVEL=INFO
FORCE_VALIDATION=false

# External URL (e.g. for ngrok)
# Set this to your ngrok URL for development webhook testing
# Example: EXTERNAL_URL=https://abc123.ngrok.dev
# EXTERNAL_URL=

# AWS Bedrock Configuration
BEDROCK_MODEL=us.anthropic.claude-3-5-haiku-20241022-v1:0
BEDROCK_REGION=us-east-2
BEDROCK_TEMPERATURE=0
BEDROCK_MAX_TOKENS=4000

# AWS Bedrock Guardrail Configuration
BEDROCK_GUARDRAIL_ID=
BEDROCK_GUARDRAIL_VERSION=
BEDROCK_GUARD_LAST_TURN_ONLY=true
BEDROCK_GUARDRAIL_TRACE=enabled

# Bedrock Knowledge Base and Retrieval Configuration
BEDROCK_KB_ID=
BEDROCK_RETRIEVAL_RESULTS=5
BEDROCK_MIN_SCORE_CONFIDENCE=0.0
